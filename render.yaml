services:
  - type: web
    name: hr-management-system
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
